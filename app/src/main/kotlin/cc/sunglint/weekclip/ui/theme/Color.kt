package cc.sunglint.weekclip.ui.theme

import androidx.compose.ui.graphics.Color

// Placeholder palette. The real tokens come from weekclip-design-system when
// feature work starts (Phase 5) — these exist only so the skeleton compiles
// and renders something honest.
val WeekclipInk = Color(0xFF101014)
val WeekclipPaper = Color(0xFFFAFAFA)
val WeekclipAccent = Color(0xFF3B6FE0)
